---
export interface TickerItem {
  symbol: string;
  label: string;
  price: number;
  change: number;
  changePercent: number;
}

interface Props {
  items: TickerItem[];
}

const { items } = Astro.props;

function formatPrice(value: number): string {
  return new Intl.NumberFormat("ko-KR", { maximumFractionDigits: 2 }).format(value);
}
---

<div class="border-b border-outline-variant/50 bg-surface-container-high/90 px-4 py-2 backdrop-blur-sm">
  <div class="mx-auto flex w-full max-w-6xl flex-wrap items-center gap-x-6 gap-y-1.5 font-mono text-[11px] tracking-tight">
    {
      items.map((item) => {
        const directionClass =
          item.change > 0 ? "text-market-up" : item.change < 0 ? "text-market-down" : "text-on-surface-variant";
        const sign = item.change > 0 ? "+" : "";
        return (
          <div class="flex items-center gap-1.5 text-on-surface">
            <span class="text-on-surface-variant">{item.label}</span>
            <span class="font-semibold tabular-nums">{formatPrice(item.price)}</span>
            <span class={`tabular-nums font-medium ${directionClass}`}>
              {sign}
              {item.changePercent.toFixed(2)}%
            </span>
          </div>
        );
      })
    }
  </div>
</div>
