apiVersion: v1
kind: Service
metadata:
  name: ziin-dashboard
  labels:
    app: ziin-dashboard
spec:
  selector:
    app: ziin-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
