apiVersion: apps/v1
kind: Deployment
metadata:
  name: ziin-dashboard
  labels:
    app: ziin-dashboard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ziin-dashboard
  template:
    metadata:
      labels:
        app: ziin-dashboard
    spec:
      containers:
        - name: web
          image: ghcr.io/your-org/ziin-dashboard:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
