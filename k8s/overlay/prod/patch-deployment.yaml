apiVersion: apps/v1
kind: Deployment
metadata:
  name: signal-agent
  namespace: signal-agent
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: web
          imagePullPolicy: Always
