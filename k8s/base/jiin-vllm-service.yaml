apiVersion: v1
kind: Service
metadata:
  name: jiin-vllm
  namespace: signal-agent
  labels:
    app.kubernetes.io/name: jiin-vllm
    app.kubernetes.io/component: inference
spec:
  type: ClusterIP
  selector:
    app: jiin-vllm
  ports:
    - name: http
      port: 8000
      targetPort: http
