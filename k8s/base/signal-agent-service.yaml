apiVersion: v1
kind: Service
metadata:
  name: signal-agent
  namespace: signal-agent
  labels:
    app.kubernetes.io/name: signal-agent
    app.kubernetes.io/component: web
spec:
  selector:
    app: signal-agent
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
